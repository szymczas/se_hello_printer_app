language: python
services:
  - docker
python:
  - "2.7"
install:
  - make deps
script:
  - make lint
  - make test
  - make docker_build
  - make docker_push
deploy:
  provider:  heroku
  app:  afternoon-dawn-07145
  api:  ${HEROKU_API_KEY}
